language: android
sudo: required
jdk:
  - oraclejdk8
install: true
before_deploy:
  # Set up git user name and tag this commit
  - git config --local user.name "sudtanj"
  - git config --local user.email "sudono007@gmail.com"
  - git tag "$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)"
deploy:
  provider: releases
  api_key: "815cb00bef5576a5aea0871f811906d758a34e10"
  file: "$TRAVIS_BUILD_DIR/outputs/apk/app-release.apk"
  skip_cleanup: true
before_install:
- yes | sdkmanager "platforms;android-27"
before_script:
  - chmod +x gradlew
script:
  - ./gradlew assembleRelease --stacktrace --info
android:
  components:
    # Uncomment the lines below if you want to
    # use the latest revision of Android SDK Tools
    - tools
    - platform-tools

    # The BuildTools version used by your project
    - build-tools-27.0.2

    # The SDK version used to compile your project
    - android-15

    # Additional components
    - extra-google-google_play_services
    - extra-google-m2repository
    - extra-android-m2repository
    - addon-google_apis-google-26

    # Specify at least one system image,
    # if you need to run emulator(s) during your tests
    - sys-img-armeabi-v7a-android-27
    - sys-img-armeabi-v7a-android-26
    - sys-img-armeabi-v7a-android-25
    - sys-img-armeabi-v7a-android-24
    - sys-img-armeabi-v7a-android-23
    - sys-img-armeabi-v7a-android-22
    - sys-img-armeabi-v7a-android-21
    - sys-img-armeabi-v7a-android-20
    - sys-img-armeabi-v7a-android-19
    - sys-img-armeabi-v7a-android-18
    - sys-img-armeabi-v7a-android-17
    - sys-img-armeabi-v7a-android-16
    - sys-img-armeabi-v7a-android-15
